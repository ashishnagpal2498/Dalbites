# Removing Pipeline from this branch 
stages:          # List of stages for jobs, and their order of execution
  - test
  - build
  - publish_image # Add image to DockerHub
  
variables:
  BACKEND_IMAGE:  maven:3.8.3-openjdk-17

trial:
   stage: test
   image: $BACKEND_IMAGE
   script:
     - cd Backend
     - pwd
   only:
     - develop
     - staging
     - backend

